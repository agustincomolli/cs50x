# Para crear una tabla.
CREATE TABLE tabla (
    columna TIPO,
    columna TIPO,
    ...
    PRIMARY KEY (columna),
    FOREING KEY (columna) REFERENCES tabla(columna)
);
CREATE TABLE books (
    id INTEGER,
    title TEXT,
    published DATE,
    rating INTEGER,
    PRIMARY KEY (id)
);

# Para insertar datos en una tabla.
INSERT INTO tabla (columna, columna...) VALUES (valor, valor...)
INSERT INTO favorites (language, problem) VALUES ('SQL', 'Fiftyville');

# Para eliminar datos en una tabla.
DELETE FROM tabla WHERE columna = valor;
DELETE FROM  favorites WHERE language = 'SQL';

# Para actualizar datos de una tabla.
UPDATE tabla SET columna = valor WHERE condición;
UPDATE favorites SET problem = 'FiftyVille' WHERE language is 'SQL';
